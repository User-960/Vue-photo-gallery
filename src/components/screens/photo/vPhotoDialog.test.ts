import { render, screen } from '@testing-library/vue'

import vPhotoDialog from './vPhotoDialog.vue'

describe('test vPhotoDialog component', () => {
	it('render component', () => {
		// given / arrange

		// when / act
		render(vPhotoDialog)

		// then / assert
	})
})
